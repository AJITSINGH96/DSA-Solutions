#pragma once
using namespace std;
#include<iostream>
class strings {
public:
	int strlength(string st);
	void ChangeCase(string s);
	bool valid(string s);
	void reverse(string s);
	void Duplicates(string s);
};

